<Window x:Class="LwpTerm.App.Views.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LWP-TERM" SizeToContent="Height" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{StaticResource Brush.Bg}"
        TextElement.Foreground="{StaticResource Brush.Text}" FontSize="13">
    <StackPanel Margin="16">
        <TextBlock x:Name="PromptText" TextWrapping="Wrap" Foreground="{StaticResource Brush.TextDim}" />
        <TextBox x:Name="ValueBox" Margin="0,10,0,0" Padding="4,3"
                 Background="{StaticResource Brush.Panel}" Foreground="{StaticResource Brush.Text}"
                 BorderBrush="{StaticResource Brush.Border}" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="OK" Width="84" Height="26" IsDefault="True" Click="OnOk" />
            <Button Content="Cancel" Width="84" Height="26" Margin="8,0,0,0" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
