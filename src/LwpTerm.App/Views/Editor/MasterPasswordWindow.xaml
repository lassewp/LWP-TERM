<Window x:Class="LwpTerm.App.Views.Editor.MasterPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LWP-TERM — unlock" Height="220" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" ShowInTaskbar="True"
        Background="{StaticResource Brush.Bg}"
        TextElement.Foreground="{StaticResource Brush.Text}" FontSize="13">
    <StackPanel Margin="18">
        <TextBlock Text="Enter your master password to unlock saved credentials."
                   TextWrapping="Wrap" Foreground="{StaticResource Brush.TextDim}" />
        <PasswordBox x:Name="PasswordBox" Margin="0,14,0,0" Padding="6,4"
                     Background="{StaticResource Brush.Panel}" Foreground="{StaticResource Brush.Text}"
                     BorderBrush="{StaticResource Brush.Border}"
                     KeyDown="OnPasswordKeyDown" />
        <TextBlock x:Name="ErrorText" Foreground="{StaticResource Brush.Error}" Margin="0,8,0,0" TextWrapping="Wrap" Visibility="Collapsed" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Unlock" Width="90" Height="28" IsDefault="True" Click="OnUnlock" />
            <Button Content="Quit" Width="90" Height="28" Margin="8,0,0,0" IsCancel="True" Click="OnQuit" />
        </StackPanel>
    </StackPanel>
</Window>
